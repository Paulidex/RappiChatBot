GEMINI_API=your-gemini-api-key-here
LLM_MODEL_NAME=gemini-model-here
DB_PATH=data/rappi_insights.db
METRIC_FILE_PATH=path-with-data
ORDER_FILE_PATH=path-with-data

# Threshold for week-over-week anomaly detection (e.g. 0.10 = 10% drop)
ANOMALY_THRESHOLD=0.10
# Minimum consecutive weeks to identify a trend
TREND_MIN_WEEKS=3
